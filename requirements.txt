langgraph==0.2.28
httpx==0.27.0
beautifulsoup4==4.12.3
python-dotenv==1.0.1

# Optional: enables Claude reasoning (attack planning + executive summary).
# The tool runs fine without a key, falling back to deterministic heuristics.
anthropic==0.39.0
